{
  "name": "bunkerpass-desktop",
  "version": "0.1.0",
  "description": "DrivePass Desktop App",
  "main": "dist/main.js",
  "scripts": {
    "prestart": "npm run build",
    "start": "electron .",
    "build": "tsc && cp src/index.html dist/index.html"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "electron": "^28.0.0",
    "typescript": "^5.0.0"
  }
}
